#Line 2 DEFINES the function say_hello()
def say_hello(name):
  print("Hello " + name)

#Line 6 CALLS the function and PASSES in the ARGUMENT "AL" to a LOCAL VARIABLE name. VARIABLES that have ARGUMENTS assigned to them are called PARAMETERS
say_hello("Al")